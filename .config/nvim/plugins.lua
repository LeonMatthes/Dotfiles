require("catppuccin").setup({
    dim_inactive = {
      enabled = true,
      shade = "dark",
      percentage = 0.15,
    }
  })

require("outline").setup({
  outline_window = { auto_close = true }
})
